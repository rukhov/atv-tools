# Copyright 2011,2012,2016,2018,2019 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-ATV
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

########################################################################
# Setup library
########################################################################
include(GrPlatform) #define LIB_SUFFIX

set(CMAKE_CXX_STANDARD_REQUIRED On)
set(CMAKE_CXX_STANDARD 23)
set(CMAKE_CXX_EXTENSIONS Off)

list(APPEND ATV_sources 
    TvDecoder_impl.cc
    RTSPServer.cpp
)

set(ATV_sources
    "${ATV_sources}"
    PARENT_SCOPE)
if(NOT ATV_sources)
    message(STATUS "No C++ sources... skipping lib/")
    return()
endif(NOT ATV_sources)

add_library(gnuradio-ATV SHARED ${ATV_sources})
target_link_libraries(gnuradio-ATV 
    PUBLIC gnuradio::gnuradio-runtime
    PUBLIC lib-atv-tools::lib-atv-tools
    PRIVATE lib_rtsp_streamer
)
target_include_directories(
    gnuradio-ATV
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    PUBLIC $<INSTALL_INTERFACE:include>)
set_target_properties(gnuradio-ATV PROPERTIES DEFINE_SYMBOL "gnuradio_ATV_EXPORTS")

if(APPLE)
    set_target_properties(gnuradio-ATV PROPERTIES INSTALL_NAME_DIR
                                                    "${CMAKE_INSTALL_PREFIX}/lib")
endif(APPLE)

########################################################################
# Install built library files
########################################################################
include(GrMiscUtils)
gr_library_foo(gnuradio-ATV)

########################################################################
# Print summary
########################################################################
message(STATUS "Using install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Building for version: ${VERSION} / ${LIBVER}")

########################################################################
# Build and register unit test
########################################################################
include(GrTest)

# If your unit tests require special include paths, add them here
#include_directories()
# List all files that contain Boost.UTF unit tests here

list(APPEND test_ATV_sources
)
# Anything we need to link to for the unit tests go here
list(APPEND GR_TEST_TARGET_DEPS gnuradio-ATV)

if(NOT test_ATV_sources)
    message(STATUS "No C++ unit tests... skipping")
    return()
endif(NOT test_ATV_sources)

foreach(qa_file ${test_ATV_sources})
    gr_add_cpp_test("ATV_${qa_file}" ${CMAKE_CURRENT_SOURCE_DIR}/${qa_file})
endforeach(qa_file)
